/** Max listings in the compare tray (hard product cap). */
export const MAX_COMPARE_ITEMS = 3;

/** Cap recently-viewed rows kept per owner (prune oldest beyond this). */
export const MAX_RECENT_VIEWS = 40;

/** HttpOnly cookie storing anonymous marketplace session id. */
export const MARKETPLACE_ANON_COOKIE = "siamez_mp_sid";

export const MARKETPLACE_ANON_COOKIE_MAX_AGE_SEC = 60 * 60 * 24 * 365; // 1 year
