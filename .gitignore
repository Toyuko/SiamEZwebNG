# Dependencies
node_modules

# Next.js build output (do not commit)
.next/

# Debug
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# Local env files
.env
.env*.local

# Legacy production extracts contain customer PII — never commit
data/legacy-extract/

# Database dumps contain production data — never commit
data/backups/

# Vercel (keep project.json in git for CI; ignore local link cache)
.vercel/*
!.vercel/project.json

# TypeScript
*.tsbuildinfo
next-env.d.ts
.env*
